server:
  port: ${SERVER_PORT:8080}

spring:
  docker:
    compose:
      enabled: false
  datasource:
    url: jdbc:postgresql://${DB_HOST:127.0.0.1}:${DB_PORT:5432}/${DB_NAME:studyfactory}
    username: ${DB_USERNAME:john}
    password: ${DB_PASSWORD:}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5174,http://127.0.0.1:5174}

study-presence:
  qr:
    secret: ${STUDY_PRESENCE_QR_SECRET:c3R1ZHlmYWN0b3J5LWRldmVsb3BtZW50LXFyLXNlY3JldC1rZXktMjAyNg==}
  auto-close:
    enabled: ${STUDY_PRESENCE_AUTO_CLOSE_ENABLED:true}
